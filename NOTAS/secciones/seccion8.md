## SECCION 8
## ESTA SECCION ESTA BASADA EN EL MANEJO Y CREACION DE ORDENES
## PASOS:
1. Definimos la interfaz de ordenes (interface/order.ts)
2. Definimos el modelo de ordenes (models/Order.ts)
3. Creamos un REST para crear una orden (api/orders/index.ts)
4. Creamos la funcion para crear una orden (context/cart)
5. Aplicamos unas validaciones para la creacion de una orden (pages/checkout/summary.tsx)
6. Aplicamos la funcion para crear una orden (pages/checkout/summary.tsx)
7. Creamos una nueva accion para vaciar el carrito (cartReducer.ts)
8. Le agregamos funcionalidad a 'Mis Ordenes' (components/ui/SideMenu.tsx)
9. Creamos un SSR en la pagina de ordenes (pages/orders/[id].tsx)
10. Reemplazamos los datos por los que vienen en la orden (pages/orders/[id].tsx)
11. Creamos un SSR en la pagina de historial de ordenes (pages/orders/history.tsx)